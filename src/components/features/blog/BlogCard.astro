---
import type { BlogSchema } from 'src/content/config';

interface Props {
    slug: string;
    blog: BlogSchema;
}

const { blog, slug } = Astro.props;
---

<div class="card">
    <a href={`/blog/${slug}`}>
        <!-- //TODO - show tags -->
        <div class="card-image">
            <img src={blog.coverImage || "/src/assets/noimage.png"} />
        </div>
        <div class="card-content">
            <h2>{blog.title}</h2>
        </div>
    </a>
</div>

<style>
    .card {
        display: flex;
        flex-direction: column;
        border: 1px solid #e5e7eb;
        border-radius: 1rem;
        box-shadow: 0 1px 4px rgba(0, 0, 0, 0.1);
    }
    .card-image {
        border-bottom: 1px solid #e5e7eb;
    }
    .card-image img {
        width: 100%;
        height: 200px;
        object-fit: contain;
    }
    .card-content {
        padding: 10px;
    }
    h2 {
        margin: 0;
    }
    a {
        text-decoration: none;
        color: #111827;
    }
    .button {
        background-color: #f1f1f1;
        padding: 10px;
        border-radius: 2rem;
        text-decoration: none;
        color: #111827;
    }
    img {
        height: 1em;
    }
</style>
